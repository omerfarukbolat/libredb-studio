# ============================================
# LibreDB Studio - Environment Configuration
# ============================================
#
# LOCAL DEVELOPMENT:
#   Copy this file to .env.local and fill in your values
#   cp env.example .env.local
#
# RENDER DEPLOYMENT:
#   Set these variables in Render Dashboard → Environment
#   https://dashboard.render.com → Your Service → Environment
#
# ============================================

# ============================================
# AUTHENTICATION (Required)
# ============================================
# Password for admin access (full access + maintenance tools)
ADMIN_PASSWORD=your_secure_admin_password

# Password for user access (query execution only)
USER_PASSWORD=your_secure_user_password

# JWT Secret for session management (min 32 characters)
# Generate one: openssl rand -base64 32
JWT_SECRET=your_32_character_or_longer_random_string

# ============================================
# AI ASSISTANT CONFIGURATION (Optional)
# ============================================
# LLM Provider: gemini, openai, ollama, or custom
LLM_PROVIDER=gemini

# API Key for the selected provider (not needed for ollama)
LLM_API_KEY=your_api_key_here

# Model name (defaults vary by provider)
# Gemini: gemini-2.5-flash, gemini-2.0-flash, gemini-1.5-pro
# OpenAI: gpt-4o, gpt-4o-mini, gpt-4-turbo
# Ollama: llama3.2, codellama, mistral
LLM_MODEL=gemini-2.5-flash

# Custom API URL (only for ollama or custom providers)
# Ollama: http://localhost:11434/v1
# LiteLLM: http://localhost:4000/v1
# LMStudio: http://localhost:1234/v1
# LLM_API_URL=http://localhost:11434/v1

# ============================================
# RENDER SPECIFIC (Auto-set by Render)
# ============================================
# PORT=3000          # Render sets this automatically
# NODE_ENV=production # Render sets this automatically
